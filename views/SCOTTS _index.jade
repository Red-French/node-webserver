SCOTT'S index.jade

doctype html
html
  head
    title= title
  body
    h1 Welcome to #{title}
    p The date is now #{date.toLocaleDateString()}
    p Breaking news:&#32;
      a(href=topStory.url)= topStory.title


doctype html
html
  head
    title= title
  body
    h1 Welcome to #{title}
    p The date is now #{date.toLocaleDateString()}
    p Breaking news:
      =" "
      a(href=topStory.url)= topStory.title


doctype html
html
  head
    title= title
  body
    h1 Welcome to #{title}
    p The date is now #{date.toLocaleDateString()}
    p Breaking news: <a href="#{topStory.url}" />hey</a>


doctype html
html
  head
    title= title
  body
    h1 Welcome to #{title}
    p The date is now #{date.toLocaleDateString()}
    p Breaking news:
      a(href=topStory.url)= topStory.title
      | This is more text
